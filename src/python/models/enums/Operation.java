package python.models.enums;

public enum Operation {
    POWER, ADD, SUB, MULT, DIV, MOD, AND, OR, XOR, LSHIFT, RSHIFT, IDIV, AT, NOT_OP,
    LESS_THAN, GREATER_THAN, EQUALS, GT_EQ, LT_EQ, NOT_EQ, IN, NOTIN, IS, ISNOT, NOT,
    NONE,
    // ASSIGN,
    STAR,
}
