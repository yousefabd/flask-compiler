condition = False

while condition:
    inside_value = 10

after_inside = inside_value

seed = 20

while condition:
    seed = 30

after_seed = seed

while condition:
    body_only = 1
else:
    else_reads_body = body_only